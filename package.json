{
  "name": "spool",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run app/dev.ts",
    "worker:dev": "bun --watch apps/worker-shell/src/server.ts",
    "frontend:dev": "bun run --filter '@spool/frontend' dev",
    "build": "bun run --filter '@spool/core' build && bun run --filter '@spool/worker-shell' build && bun run --filter '@spool/frontend' build",
    "typecheck": "bun run --filter '@spool/core' typecheck && bun run --filter '@spool/worker-shell' typecheck && bun run --filter '@spool/frontend' typecheck",
    "test": "bun run --filter '@spool/core' test",
    "check": "bun run typecheck && bun run test && bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.2.13",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.15.2"
  },
  "dependencies": {
    "zod": "^3.25.76"
  }
}
