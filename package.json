{
  "name": "spool",
  "private": true,
  "type": "module",
  "workspaces": [
    "worker-shell",
    "frontend"
  ],
  "scripts": {
    "dev": "bun run app/dev.ts",
    "wasm:build": "wasm-pack build rust-core --target web --out-dir ../app/generated/rust-core",
    "mock:build": "cargo build -p mock-agent",
    "worker:dev": "bun --watch worker-shell/src/server.ts",
    "frontend:dev": "bun run --filter '@spool/frontend' dev",
    "build": "bun run wasm:build && bun run --filter '@spool/frontend' build && bun run --filter '@spool/worker-shell' build",
    "typecheck": "bun run --filter '@spool/worker-shell' typecheck && bun run --filter '@spool/frontend' typecheck",
    "check": "bun run typecheck && cargo fmt --check && cargo test --workspace"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.15.2"
  }
}
